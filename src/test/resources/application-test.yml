spring:
  datasource:
    url: jdbc:h2:mem:fcl_test_${random.uuid};MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect

fcl:
  security:
    jwt:
      secret: test-secret-key-at-least-32-bytes-long-12345
